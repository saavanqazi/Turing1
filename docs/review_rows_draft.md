# review.csv — row drafts to paste into the review form

Paste these into the review form (not into a hand-written file). All figures are filled in from the recorded runs. Keep the row names exactly.

| review_check | status | review_notes | change_made | what_to_record |
|---|---|---|---|---|
| Layer 1 · Package consistency | FIXED_AND_VERIFIED | task.toml, instruction.md, submission_format.md and tests/manifest.json name the same two deliverables (offer_evaluation.csv, results.json). Mined package shipped tests/verifier.json and loaded it by name in score.py:38 and test_outputs.py:41; gold, golden_trajectory.json and verifier expected values were three hand-kept copies of one answer; evaluations/ held oracle/ and nop/. | Renamed tests/verifier.json to tests/manifest.json and updated both loaders. Added solution/compute_gold.py which regenerates solution/files, golden_trajectory.json and the manifest expected values from environment/input in one run. Removed evaluations/oracle and evaluations/nop. Dropped the stale consistency/ folder from the bundle. | Re-ran compute_gold.py, replayed gold through tests/score.py (1.0, 6/6) and tests/test_outputs.py (12 lanes green); oracle harbor jobs oracle-b7a4-r1 and oracle-b7a4-r2 (final graded files) reward 1.0 after the rename. |
| Layer 1 · Clarity and scope | FIXED_AND_VERIFIED | Cold read produced five guesses (whose clock for cutoff, fee taxed or not, rounding order, is 10 ft exact, precedence on double failure); each is settled by an explicit clause in same_day_terms.md. Instruction rewritten to name the two new inputs (stock ledger, tax table) in natural voice, 175 words, no paths, no method, no counts. | instruction.md paragraph rewritten; same_day_terms.md S1 defines the store order instant, S2 defines effective stock and NO_STOCK, S6 defines destination-based tax for delivery; submission_format.md allowed values gained NO_STOCK. | Re-read instruction, terms and format side by side after the edit; every reason code in the gold appears in the format doc; GLM failure classification across the 4 final runs: the one failure (difficulty/r1, OF-13 precedence slip) is MODEL, not ambiguity; S5 states the order and the same run applied it correctly on OF-19. |
| Layer 1 · Realism and leakage | PASS | Prompt states no totals, counts, thresholds or method; verifier names never enter the image (Dockerfile copies input/ only). Kestrel, stores and rates are fictional; the Columbus GA / Phenix City AL state-line timezone split is real geography. Standard_delivery.md is a realistic distractor. | | Grepped instruction.md and input/ for the gold figures (7, OF-12, 18.62): none present outside the computed rows; checked Dockerfile COPY lines. |
| Layer 2 Difficulty | FIXED_AND_VERIFIED | Mined baseline not batteried separately; round 1, a strict superset, passed 4/4 so the baseline is at least that easy. Cause: eight independent rules, one trap, STOCK_RESERVE and tie-breaks never firing. Round 1 (ledger, destination tax, tie) still 4/4 (rewards 1.0,1.0,1.0,1.0; job glm-b7a4-r1): GLM scripts explicit rules reliably. Final battery 3/4, rewards 0.0, 1.0, 1.0, 1.0 in evaluations/difficulty/r1..r4/verifier/reward.json (harbor job glm-b7a4-r2, trials task__EGsQHvh, LZ5ZyDc, QMJpLL8, vR6K4Kt). The failing run reasoned by hand, got 19/20 rows and reported OF-13 as SAME_DAY_SUSPENDED where STOCK_RESERVE precedes it: MODEL failure. | Round 1: stock_ledger.csv (store-local timestamped movements, NO_STOCK), tax_jurisdictions.csv with destination-based tax on delivery, a sixth store creating a same-cent tie resolved by distance, precedence edge SKUs/offers (20 offers, 10 SKUs). Round 2: unsigned ledger qty with event direction and a VOID, supplier-part aliases, prior-day lines inside OPENING, store_notices.md overriding cutoff and pausing a method, duplicate ST-23 row with effective_from, fit thresholds moved to power_supply_spec.md, half-cent-up rounding on the chosen offer (17.00 x 1.095 = 18.615). Terms S1-S6, submission_format.md, instruction.md updated; gold regenerated by compute_gold.py. | Oracle re-run 1.0 after each round (oracle-b7a4-r1, oracle-b7a4-r2), then a fresh 4-run GLM-5.2 battery from clean containers per round (glm-b7a4-r1 4/4, glm-b7a4-r2 3/4); each failing run's score.json and trajectory read in full; rewards recorded above. |
| Layer 2 Solvability | PASS | evaluations/solvability/r1 is a copy of difficulty run r2, harbor trial task__LZ5ZyDc (GLM-5.2, terminus-2 harness), reward 1.0, not an oracle. | | Read solvability/r1/verifier/reward.json and result.json (model GLM-5.2, overall_pass true). |
| Layer 2 Stability | PASS | Turing runs this. | | Turing runs stability. |
| Layer 3 Oracle Mode | FIXED_AND_VERIFIED | Mined package's shipped oracle evidence 1.0 (2026-09-20) reproduced by a local engine replay of the gold. Gold changed with hardening so the oracle assets had to be regenerated; solve.sh still installs solution/files and emits the ATIF trajectory. | solution/files/*, solution/golden_trajectory.json and tests/manifest.json expected values regenerated by solution/compute_gold.py. | Oracle re-run after each hardening round and once more after the manifest rename: jobs oracle-b7a4-r1 and oracle-b7a4-r2 (final graded files), both reward 1.0; local engine replay 1.0 and 12 test_outputs.py lanes green. |
| Layer 4 · Environment and files | PASS | Dockerfile: python:3.12-slim, pinned engine deps, COPY input/ only, chmod a-w on /app/input; no network needed at run time. All ten files the instruction refers to exist in environment/input. | | Built and ran the image via the oracle and GLM harbor jobs; the GLM trajectories list /app/input and show all ten files. |
| Layer 4 · Connectors, MCPs, and CLIs | N/A | Non-connector task: task.toml mcp_servers = [], no environment/_app mirror, no connector manifest. | | |
| Layer 4 · Deliverables and artifact quality | PASS | Two deliverables, both plain (CSV with pinned header, JSON with closed key set). submission_format.md pins header, allowed values (incl. NO_STOCK), number rendering and gives placeholder examples. Gold files conform. | | Replayed gold through the engine (header regex, table_equals, object_equals all pass); checked landed_cost_usd is empty on ineligible rows and two-decimal on eligible ones. |
| Layer 5 · Verifier coverage and fairness | PASS | Forward map: sheet exists+header (2 checks), every row's decision/reason/cost (evaluation_table + evaluation_table_trap_of01, disjoint rows), count/chosen/total (results_figures) + exists. Backward: no check grades an unasked thing. Only regex is the header check, case- and quote-tolerant. Numeric cells ±0.001, total ±0.01; row order and column order free. | | Listed instruction asks against manifest checks both directions; read compare.table_equals for tolerance and ordering behaviour; confirmed cosmetic re-renderings (quoted header, 18.62 vs 18.620) still score 1.0. |
| Layer 5 · LLM judge consistency | N/A | No judged rubric: all six checks are deterministic file comparisons; JUDGE_MODEL is not consulted. | | |
| Layer 5 · Reward hacking and exploitability | PASS | results.json key set closed; offer_evaluation.csv column set closed and row_set locked to the 20 offer ids (duplicate or cross-product dumps fail); tests/solution never enter the image; no network or state to spoof. test_outputs.py negative lanes prove deleting a file, corrupting any figure or adding a key drives reward to 0. | | Ran tests/test_outputs.py on the gold workspace: 12 lanes green including incomplete, corrupted-value and extra-key lanes. |
| Cross-trial · Calibration | PASS | Turing runs this. Observed locally: the single failing run missed one cell (OF-13 precedence), one cause. | | Turing runs calibration. |
